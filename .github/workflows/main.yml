name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  call-reusable-workflow:
    uses: kirandevops1993/Repo-B/.github/workflows/reusable-ci.yml@main
    with:
      repo_url: ${{ secrets.REPO_A_URL }}
